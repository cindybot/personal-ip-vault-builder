---
name: personal-ip-vault-builder
description: "建立、整理、檢視或遷移個人 IP 知識庫（personal brand Vault），以可追溯的本人資料支援職涯展示、內容創作、私密反思及帳號管理。用於 Vault 結構與資料治理；單次改 CV 或寫 post 而不涉及知識庫結構時不需啟用。"
---

# Personal IP Vault Builder

建立以本人資料為核心、來源可追溯、容易查找的 Markdown Vault。沿用使用者已確認的用途和界線；未知個人資料保留 TBC。

## 判斷工作範圍

- 新建：先確認目標資料夾，建立首個用途需要的最小結構。
- 檢視：提出有檔案證據的問題及調整建議；純檢視不授權改寫。
- 遷移／調整：保留本人資料、來源及仍有用途的工具，按已授權方案修正分類和引用。

已在當前對話確認的決定不用重問。使用者要求先看方案、名稱或差異時，停在該指定檢閱點；普通本地建構步驟可在既有授權內完成。

## 1. 讀取與補齊關鍵選擇

先讀目標 Vault 的 AGENTS.md、INDEX.md、私隱政策及直接相關來源。私密目錄、原始文件、Archive 及其他 Projects 不預載，也不為了 audit 全庫讀入。

按缺口詢問會改變架構的事項：

- 首個實際用途，例如整理背景、CV、個人網站、內容創作或服務。
- 已有資料位置、可否存原檔，以及同步／容量選擇。
- 法律身份與公開名稱如何使用；敏感資料的保存、讀取、引用及發布界線。
- 需要哪些公開 Profile；帳號數目、平台、定位、語言可以保持未定。

毋須要求所有選項都定好才建庫。Profile 不等於帳號；專業／創作雙 Profile 是一種選擇，不是所有人的固定配置。只有使用者需要時才加入服務區或私密反思區。

## 2. 盤點及遷移

讀 [結構與空白模板](references/blueprint.md)，以用途判斷檔案歸屬。

先列檔名和來源，再針對允許範圍讀取內容，分辨：

- 本人的事實、作品、研究或決定。
- 仍可重用的流程、工具及設定。
- 已確認無關的範例、舊身份或過時輸出。

公司名稱、舊 folder 名稱或「demo」字眼本身不足以證明整個 folder 可以刪除。本人曾任職的機構、相關研究和混合資料夾要逐項處理；通用 Skill 亦不是範例公司資料。

在改動前整理精簡對照：原路徑、目的地／處理方式、保留內容、需更新的引用。查明目的地是否已有內容，避免覆寫。變更完成後才移除已合併的重複檔。

刪除只針對已明確授權的檔案；不可因為有副本而擴大清理範圍。遇到禁止永久刪除的執行限制，可在授權範圍內用垃圾桶並如實回報；不可改用其他程式繞過限制，也不可清空整個垃圾桶。

## 3. 建構與寫入

依 [blueprint](references/blueprint.md) 選用需要的區域，不預建所有平台、帳號、Projects 或空白卡片。

維持以下關係：

- 原始來源 → 來源記錄 → 唯一負責的個人事實檔 → 公開 Profile／CV／內容。
- 法律姓名獨立私密存放；公開名稱有自己的檔案。公開 Profile 不連到私密身份檔。
- `sources`、`verification`、`visibility`、`publication` 各有用途；已核實不等於可發布。
- 建庫狀態只在 VAULT-STATUS.md 管理；索引只提供路由。
- 詳細私隱規則只在一份使用者政策檔管理，AGENTS 保留必要操作限制並指向該檔。

每份 owner 決定或文件來源建立可追溯 receipt；來源登記不會自動確認其所有內容。多來源支持的事實逐項連回來源，衝突保留為待確認。

語言、人物、職稱、機構、服務及日期由當前使用者提供；不複製其他人的個人資料、已確認偏好、帳號或本機路徑作為新 Vault 預設。

## 4. 驗收

以此 Skill 資料夾為工作目錄執行：

```bash
python3 scripts/validate_vault.py /absolute/path/to/the-vault
```

如有額外私密／原始目錄，必須先以重複的 `--exclude relative/path` 排除。遷移時可用 `--legacy-term KNOWN_OLD_IDENTIFIER` 檢查已知舊識別；不要把任職機構名一律當作殘留。

工具只讀檔案：檢查入口、一般 Markdown 行內連結、可讀範圍的舊識別；預設略過私密目錄、標為 private 的筆記內文、原始文件、Archive、隱藏檔及 symlink。報告列出檢查範圍，不能推論未讀範圍沒有敏感內容。Wiki／reference links 會報為需人工檢查；不驗證 anchor、外站、內容真實性或發布權。

另外人工核對：

- 私密身份與公開名稱已分檔，公開 Profile 沒有私密身份連結。
- source、個人事實、公開敘事、帳號資料各有歸屬，進度及政策沒有多份權威副本。
- 採用使用者提供並允許使用的一項資料，走一次首個用途的來源追蹤與草稿路徑。

若尚未有實際 source，明確回報「結構可用，實際資料流程待驗收」，不假造履歷或故事完成示範。

交付入口檔、改動摘要、驗證結果與範圍、清理恢復方式（如有）、未決事項及下一步。

## 可分享的 Skill 套件

只有使用者要求分享／GitHub 儲存時才處理套件：

- 在 Vault 外建立通用套件，只包含 Skill、空白模板、驗證程式及使用說明。
- 逐項核對待分享檔案；不打包實際 Vault、來源文件、owner 決定、私密資料、Obsidian 狀態、密碼或本機絕對路徑。
- GitHub owner、repo、可見性與待發布版本要有明確依據；沿用已給的授權，未定事項才詢問。
- 只上傳核對過的檔案，完成後 read-back repo 路徑／commit。不得把整個私人 Vault 做成分享 repo。
